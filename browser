#!/bin/bash

set -e

self () {
	local SELF="$(basename -- "$0")"
	case "$SELF" in
	web|browser)
		SELF="${BROWSER:-safari}"
		;;
	esac
	echo "$SELF"
}

if [ $# -eq 0 ]; then
	if [ -t 0 ]; then
		usage "$0"; exit
	fi
	set -- "$(to_tmp)"
fi
open -a "$(self)" "$@"
